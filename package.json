{
  "name": "sst-api-auth-cognito",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.91.0"
  },
  "dependencies": {
    "@serverless-stack/cli": "0.9.9",
    "@serverless-stack/resources": "0.9.9",
    "@aws-cdk/core": "1.91.0"
  }
}
